"""Execution module — a deliberately-empty, default-OFF placeholder (Phase 5, not built).

Detection is the product; order placement is out of scope for this showcase. When built, this
package will hold ``guard.py`` (EXECUTION_ENABLED + max-notional cap + kill-switch + per-trade
confirm) and ``executor.py`` (multi-leg submission, only ever behind the guard). Nothing here is
importable or on the default scan path today. See SPEC.md §"Phase 5".
"""
